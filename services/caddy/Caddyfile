localhost {
  log {
    format json
  }

  # Компрессия
  encode zstd
  # Самоподписной сертификат для локальной разработки
  tls internal

  # Проксирование всех запросов в контейнер с приложением
  reverse_proxy /* app:8080
}